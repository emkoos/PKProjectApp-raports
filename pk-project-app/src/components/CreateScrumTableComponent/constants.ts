export interface IForm {
    localisation: number;
}

export interface Board{
    id: string;
    name: string;
    teamId: string;
    boardTypeId: string;
}

export interface Team{
    id: string;
    name: string;
}
